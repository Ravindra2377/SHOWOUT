"use client";
import { useState } from "react";
import Link from "next/link";
import { ArrowLeft, MoreHorizontal, Plus, Send } from "lucide-react";
import { inbox, messages as initial } from "@/lib/demo-data";

export function Conversation({id}:{id:string}){const person=inbox.find(x=>x.id===id)??inbox[0];const [messages,setMessages]=useState(initial);const [body,setBody]=useState("");const send=()=>{const clean=body.trim().slice(0,1500);if(!clean)return;setMessages([...messages,{id:crypto.randomUUID(),mine:true,body:clean,time:"now"}]);setBody("");};return <div className="page-narrow conversation-page"><header className="conversation-head"><Link className="icon-button" href="/inbox" aria-label="Back to inbox"><ArrowLeft size={18}/></Link><div><strong>{person.displayName}</strong><br/><small>@{person.handle} · {person.context}</small></div><button className="icon-button" aria-label="Conversation actions: mute, leave, block or report"><MoreHorizontal size={18}/></button></header><div className="message-list" aria-live="polite"><button className="button ghost" style={{alignSelf:"center",minHeight:36}}>Load older messages</button><div className="notice" style={{alignSelf:"center",fontSize:11}}>Messaging unlocked by shared challenge participation. Keep it constructive.</div>{messages.map(message=><div className={`message ${message.mine?"mine":""}`} key={message.id}><p>{message.body}</p>{"reaction" in message&&message.reaction&&<span className="stamp" style={{marginTop:7}}>{message.reaction}</span>}<time>{message.time}</time></div>)}</div><div className="composer"><button className="icon-button" aria-label="Share challenge, Proof or team invitation"><Plus size={19}/></button><input value={body} maxLength={1500} onChange={e=>setBody(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();send();}}} aria-label="Message" placeholder="Write a message…"/><button className="icon-button" style={{background:"var(--red)"}} onClick={send} aria-label="Send message"><Send size={18}/></button></div></div>}
