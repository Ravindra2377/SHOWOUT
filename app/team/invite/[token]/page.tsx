"use client";
import Link from "next/link";
import { Check, Clapperboard, Users, X } from "lucide-react";
import { useState } from "react";

export default function TeamInvitePage(){const [state,setState]=useState<"pending"|"accepted"|"declined">("pending");return <div className="page-narrow"><header style={{marginBottom:35}}><span className="wordmark">SHOWOUT<span className="dot"/></span></header>{state==="pending"?<article className="panel"><div style={{background:"var(--red)",padding:24,borderBottom:"var(--rule)"}}><span className="stamp">TEAM INVITATION</span><h1 className="display-sm" style={{marginTop:15}}>ROOM 42<br/>CREW.</h1></div><div className="panel-pad"><p className="eyebrow">Maya invited you</p><h2>Editor wanted for Challenge #042</h2><p>ONE ROOM. ONE MINUTE. ONE THRILLER.</p><div className="notice"><Clapperboard size={16}/> Requested role: <strong>Editor</strong><br/>Team membership freezes when the entry is submitted.</div><ul className="rule-list"><li>Maya Sen · Director</li><li>Niko Alvarez · Sound</li><li>You · Editor (invited)</li></ul><div style={{display:"grid",gridTemplateColumns:"2fr 1fr",gap:8,marginTop:18}}><button className="button lime" onClick={()=>setState("accepted")}><Check size={16}/> Join team</button><button className="button" onClick={()=>setState("declined")}><X size={16}/> Decline</button></div></div></article>:<div className="empty-state"><Users size={45}/><h1 className="display-sm">{state==="accepted"?"YOU’RE IN.":"DECLINED."}</h1><p>{state==="accepted"?"The Room 42 Crew conversation is now available in your Inbox.":"The team has been notified. This invitation link can’t be reused."}</p><Link href={state==="accepted"?"/messages/team-42":"/arcade"} className="button primary">Continue</Link></div>}</div>}
