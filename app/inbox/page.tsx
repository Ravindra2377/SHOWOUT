import Link from "next/link";
import { Search, SlidersHorizontal } from "lucide-react";
import { inbox } from "@/lib/demo-data";

export default function InboxPage(){return <div className="page-narrow"><header style={{display:"flex",justifyContent:"space-between",alignItems:"end",marginBottom:20}}><div><p className="eyebrow">Controlled connections</p><h1 className="display-sm">INBOX.</h1></div><div style={{display:"flex",gap:7}}><button className="icon-button" aria-label="Search inbox"><Search size={17}/></button><Link className="icon-button" href="/settings/messaging" aria-label="Messaging controls"><SlidersHorizontal size={17}/></Link></div></header><nav className="tabs" aria-label="Inbox sections"><Link className="active" href="/inbox">Messages</Link><Link href="/inbox/requests">Requests <strong>1</strong></Link><Link href="/inbox?tab=teams">Team conversations</Link></nav><div className="inbox-list">{inbox.filter(x=>x.kind!=="request").map(item=><Link className="inbox-item" href={`/messages/${item.id}`} key={item.id}><img src={item.avatar} alt=""/><div style={{minWidth:0}}><h3>{item.displayName} <span className="muted">@{item.handle}</span></h3><p>{item.preview}</p><small>{item.context}</small></div><div className="inbox-meta">{item.time}{item.unread>0&&<span className="unread-count">{item.unread}</span>}</div></Link>)}</div><div className="notice" style={{marginTop:22}}>Inbox activity is intentionally quiet: no online status, streaks, or read receipts. Contact is limited by shared work and your messaging settings.</div></div>}
