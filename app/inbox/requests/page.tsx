"use client";
import { useState } from "react";
import Link from "next/link";
import { ArrowLeft, Ban, Check, Flag, X } from "lucide-react";
import { inbox } from "@/lib/demo-data";

export default function RequestsPage(){const [decision,setDecision]=useState<string>();const req=inbox.find(x=>x.kind==="request")!;return <div className="page-narrow"><Link href="/inbox" className="eyebrow"><ArrowLeft size={15}/> Inbox</Link><header style={{margin:"25px 0"}}><p className="eyebrow">One introduction only</p><h1 className="display-sm">MESSAGE<br/>REQUESTS.</h1></header>{decision?<div className="empty-state"><Check size={35}/><h2 className="display-sm" style={{fontSize:38}}>{decision}.</h2><p>This request can’t be repeatedly recreated. You can adjust your controls any time.</p></div>:<article className="panel panel-pad"><div style={{display:"grid",gridTemplateColumns:"70px 1fr",gap:14}}><img src={req.avatar} alt="" style={{width:70,height:70,objectFit:"cover",border:"var(--rule)"}}/><div><h2 style={{margin:0}}>{req.displayName}</h2><p style={{margin:"3px 0"}}>@{req.handle}</p><span className="status">New request</span></div></div><p className="notice" style={{marginTop:18}}>{req.context}</p><blockquote style={{margin:"20px 0",borderLeft:"5px solid var(--red)",paddingLeft:15,lineHeight:1.5}}>“{req.preview}”</blockquote><p className="muted" style={{fontSize:12}}>Jules cannot send another message until you accept. Declining prevents this request from being recreated.</p><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:8}}><button className="button lime" onClick={()=>setDecision("Accepted")}><Check size={15}/> Accept</button><button className="button" onClick={()=>setDecision("Declined")}><X size={15}/> Decline</button><button className="button ghost" onClick={()=>setDecision("Blocked")}><Ban size={15}/> Block</button><button className="button ghost" onClick={()=>setDecision("Reported")}><Flag size={15}/> Report</button></div></article>}</div>}
