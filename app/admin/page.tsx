import { AlertTriangle, ArrowUpRight, CheckCircle2, Eye, Flag, Plus } from "lucide-react";
import { challenges } from "@/lib/demo-data";

export default function AdminPage(){return <div className="page"><header className="challenge-mast"><p className="eyebrow">Protected · pilot operators</p><h1 className="display-md">CONTROL<br/>ROOM.</h1><p className="lede">Lifecycle, moderation and pilot health. Authorization is re-checked on every server operation.</p></header><section><div className="section-head"><h2>Pilot funnel</h2><span className="stamp">Last 30 days</span></div><div className="metric-grid">{[["48%","Entered → submitted","Target ≥40%"],["74%","Submitted → Reveal","Target ≥70%"],["29%","Invited someone","Target ≥25%"],["33%","Second challenge","Target ≥30%"],["4","Started another round","Target ≥3"]].map(([value,label,target])=><div className="metric" key={label}><strong>{value}</strong><span>{label}</span><small className="target">{target}</small></div>)}</div></section><section><div className="section-head"><h2>Operator queue</h2><button className="button primary"><Plus size={15}/> New challenge</button></div><div className="programme"><div className="programme-row"><strong className="programme-time" style={{color:"var(--red)"}}>07</strong><div><h3>Entries await review</h3><p>2 upload checks · 5 content decisions</p></div><Eye/></div><div className="programme-row"><strong className="programme-time">03</strong><div><h3>Open safety reports</h3><p>Oldest report: 4h · one message reference preserved</p></div><Flag/></div><div className="programme-row"><strong className="programme-time">02</strong><div><h3>Vote integrity signals</h3><p>Abnormal speed cluster · repeated session pattern · review only</p></div><AlertTriangle/></div></div></section><section><div className="section-head"><h2>Challenge lifecycle</h2></div><div className="table-wrap"><table><thead><tr><th>Challenge</th><th>Server state</th><th>Entries</th><th>Next boundary</th><th>Action</th></tr></thead><tbody>{challenges.map(c=><tr key={c.id}><td><strong>#{c.number}</strong> {c.title}</td><td><span className={`status ${c.state==="REVEAL_LIVE"?"reveal":""}`}>{c.state}</span></td><td>{c.entryCount}</td><td>{new Date(c.submissionClosesAt).toLocaleDateString("en-GB")}</td><td><button className="icon-button" aria-label={`Edit challenge ${c.number}`}><ArrowUpRight size={16}/></button></td></tr>)}</tbody></table></div></section><section><div className="section-head"><h2>Recent moderation</h2><CheckCircle2/></div><div className="notice">All operator actions require a rationale and create an immutable audit record. Message content is never written to application logs or analytics.</div></section></div>}
