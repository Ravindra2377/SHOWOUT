import Link from "next/link";
import { Bell, ChevronRight, Eye, LogOut, MessageSquare, Shield, UserRound } from "lucide-react";

export default function SettingsPage(){return <div className="page-narrow"><header><p className="eyebrow">Account controls</p><h1 className="display-sm">SETTINGS.</h1></header><div className="programme" style={{marginTop:28}}>{[[UserRound,"Profile + account","Handle, age band and pilot access","/onboarding"],[MessageSquare,"Messaging","Who can contact you and muted conversations","/settings/messaging"],[Shield,"Safety + privacy","Blocks, reports and data controls","/settings"],[Bell,"Quiet notifications","Challenge deadlines and inbox summaries","/settings"],[Eye,"Accessibility","Motion, captions and contrast","/settings"]].map(([Icon,title,desc,href])=><Link className="programme-row" href={String(href)} key={String(title)} style={{gridTemplateColumns:"42px 1fr auto"}}><Icon size={22}/><div><h3>{String(title)}</h3><p>{String(desc)}</p></div><ChevronRight/></Link>)}</div><button className="button ghost" style={{marginTop:22}}><LogOut size={16}/> Sign out</button><p className="muted" style={{fontSize:11}}>SHOWOUT never publishes precise location or public follower counts.</p></div>}
