"use client";
import Link from "next/link";
import { ArrowLeft, Check, ShieldCheck } from "lucide-react";
import { useState } from "react";

export default function MessagingSettings(){const [setting,setSetting]=useState("SHARED");return <div className="page-narrow"><Link href="/settings" className="eyebrow"><ArrowLeft size={15}/> Settings</Link><header style={{marginTop:25}}><p className="eyebrow">Account-level control</p><h1 className="display-sm">WHO CAN<br/>REACH YOU?</h1><p className="lede">Your work may be public after Reveal. Your inbox is not.</p></header><div className="form-stack">{[["RESTRICTED","Most restrictive","Only current teams and mutually accepted connections."],["SHARED","Shared context","Teams, mutual connections, and people from the same challenge."],["REQUESTS","Allow moderated requests","Eligible creators may send one introduction for approval."]].map(([value,title,desc])=><button key={value} onClick={()=>setSetting(value)} className="panel panel-pad" style={{textAlign:"left",cursor:"pointer",background:setting===value?"var(--lime)":"white",display:"grid",gridTemplateColumns:"1fr auto",gap:10}}><span><strong>{title}</strong><br/><small>{desc}</small></span>{setting===value&&<Check/>}</button>)}<div className="notice"><ShieldCheck size={16}/> Minor accounts default to the most restrictive setting. Adult-to-minor requests require an approved shared challenge/team context or same-age mutual connection.</div><button className="button ink full">Save messaging controls</button></div></div>}
